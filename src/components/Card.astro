---
export interface Props {
  post: any;
}

const { post } = Astro.props as Props;
---

<li
  class="mx-auto py-6 w-full lg:min-h-[200px] h-full flex flex-col items-start justify-between | md:first:col-span-2 lg:col-span-2 lg:first:col-span-6 lg:third-child:col-span-3 lg:second-child:col-span-3 group first:h-[75vh] lg:first:h-96 group relative first:text-white overflow-hidden z-10 first:rounded-3xl first:p-6 border-b border-white/10 last:border-b-0"
>
  <div
    class="absolute group-first:flex hidden lg:group-first:hidden first:bg-gradient-to-t from-grstart via-grcenter to-grend h-[200%] w-[200%] lg:h-[500%] -top-[50%] -left-[50%] z-0 animate-spin-slow blur-lg"
  >
  </div>
  <!-- Закоментированный код для показа песен из разрыва танцпола -->
  <!-- <div class="flex flex-col items-start w-full justify-between">
			{post.frontmatter.song && (
			<div
				class="text-base font-medium flex flex-row items-center w-full justify-start gap-2 opacity-50"
			>
				<svg
					xmlns="http://www.w3.org/2000/svg"
					viewBox="0 0 24 24"
					class="dark:fill-white fill-black w-4 h-4 animate-pulse"
					><path
						d="M17.68 5.47H22V8h-4.32zm0 3.51H22v2.53h-4.32zm0 3.51H22v2.53h-4.32zM2 16h4.32v2.53H2zm5.22 0h4.32v2.53H7.22zm5.23 0h4.32v2.53h-4.32zm5.23 0H22v2.53h-4.32zm-5.23-3.51h4.32v2.53h-4.32zm-5.23 0h4.32v2.53H7.22zm0-3.51h4.32v2.53H7.22z"
					></path>
				</svg>
				<span>Разрыв танцпола:</span>
			</div>
			<p class="font-bold text-lg opacity-50">
				{post.frontmatter.song}
			</p>
			)}
			{!post.frontmatter.song && (
			<div
				class="text-base font-medium flex flex-row items-center w-full justify-start gap-2 opacity-50"
			>
				<svg
					xmlns="http://www.w3.org/2000/svg"
					viewBox="0 0 24 24"
					class="dark:fill-white fill-black w-4 h-4 animate-pulse"
					><path
						d="M17.68 5.47H22V8h-4.32zm0 3.51H22v2.53h-4.32zm0 3.51H22v2.53h-4.32zM2 16h4.32v2.53H2zm5.22 0h4.32v2.53H7.22zm5.23 0h4.32v2.53h-4.32zm5.23 0H22v2.53h-4.32zm-5.23-3.51h4.32v2.53h-4.32zm-5.23 0h4.32v2.53H7.22zm0-3.51h4.32v2.53H7.22z"
					></path>
				</svg>
				<span> Танцпол не разорвался :( </span>
			</div>
			)}
		</div> -->

  <div
    class="flex group-first:flex-col group-first:items-start flex-row items-end w-full justify-between relative z-10 group-first:h-full"
  >
    <a
      href={post.url}
      class="font-semibold text-2xl w-5/6 lg:group-first:text-6xl lg:group-first:font-extrabold group-first:text-5xl truncate"
    >
      {post.frontmatter.title}
    </a>
    <div
      class="flex flex-row items-center w-full justify-end gap-1 opacity-50 group-first:opacity-100 group-first:mt-auto group-first:justify-start"
    >
      <time
        datetime={post.frontmatter.pubDate}
        class="text-sm font-medium uppercase group-first:text-2xl group-first:font-bold"
      >
        {
          new Date(post.frontmatter.pubDate).toLocaleDateString("ru-RU", {
            year: "numeric",
            month: "short",
            day: "numeric",
          })
        }
      </time>
    </div>
  </div>
</li>
